

## Rename Navigation Labels

Update labels in three places:

1. **`src/components/NavBar.tsx`** — Change nav link labels:
   - "Runs" → "Initial list runs"
   - "Longlist" → "Initial list"
   - "Approach" → "Initial list approach"

2. **`src/pages/RunsPage.tsx`** — Update page heading from "Search Runs" to "Initial List Runs"

3. **`src/pages/ApproachPage.tsx`** — Update page heading from "Search Approach" to "Initial List Approach"

4. **`src/pages/LonglistPage.tsx`** / **`src/pages/GlobalLonglistPage.tsx`** — Update any heading text from "Longlist" to "Initial List"

