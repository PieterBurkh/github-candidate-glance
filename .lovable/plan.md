

## Plan: Add NavBar to Job Description Page

**File: `src/pages/JobDescription.tsx`**
- Import `NavBar` from `@/components/NavBar`
- Add `<NavBar />` at the top of the page, before the existing header
- Remove the "Back" link since the NavBar already provides navigation

